# simple url encoder/decoder
first, check if you have virtual environment, then install required libraries
```
pip3 install -r requirements
```
run server
```
uvicorn main:app --reload
```
quit server by CTRL + C